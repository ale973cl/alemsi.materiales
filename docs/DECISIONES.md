# DECISIONES — ALEMSI Materiales

Archivo **solo-agregar**. No reescribir ni borrar decisiones anteriores; si una decisión cambia, agregar una nueva entrada que indique qué reemplaza.

Formato: `fecha | decisión tomada | por qué | alternativa descartada`

| Fecha | Decisión tomada | Por qué | Alternativa descartada |
|---|---|---|---|
| 2026-09-17 (consolidación) | El presupuesto excedido solo genera alerta gerencial y nunca bloquea abastecimiento/OC. | El control presupuestario debe informar y escalar sin detener una necesidad operacional válida. | Bloqueo duro por presupuesto excedido. |
| 2026-09-17 (consolidación) | La transferencia del proyecto Supabase al cliente se realiza mediante **Transfer Project** nativo de Supabase. | Mantiene continuidad técnica y propiedad sin recrear manualmente la base. | Migración manual/recreación del proyecto como método principal de transferencia. |
| 2026-09-17 (consolidación) | Mantener `middleware.ts` en vez de `proxy.ts` mientras el proyecto permanezca en Next.js 15. | Es la convención compatible con la versión actual del framework usada por el proyecto. | Migrar anticipadamente a `proxy.ts` sin actualización de Next.js que lo justifique. |
| 2026-09-17 (consolidación) | Usar una única variable de servidor `SUPABASE_SERVICE_ROLE_KEY`. | Evita claves paralelas, ambigüedad de configuración y diferencias entre entornos. | Varias variables equivalentes para la misma service role key. |
| 2026-09-17 (consolidación) | Los maestros se presentan mediante una sola entrada funcional unificada. | Evita navegación duplicada y pérdida de acciones entre pantallas equivalentes. | Múltiples accesos/maestros paralelos que llevan al mismo propósito. |
| 2026-09-17 | `STATE.md` es el punto de continuidad técnica y se actualiza en el mismo commit de cada tarea. | Separar memoria funcional de estado técnico verificable y permitir continuidad entre sesiones. | Depender del historial del chat o de memoria para reconstruir el último estado. |
| 2026-09-17 | Se precisa que el proyecto compila en Next.js `15.5.x`, por lo que el archivo raíz de sesión debe seguir siendo `middleware.ts`. | `proxy.ts` corresponde a la convención de Next 16 y no ejecuta la sesión correctamente en la versión actual. | Usar `proxy.ts` mientras el proyecto siga en Next.js 15.5.x. |
| 2026-09-17 | La única variable válida para la clave privada de Supabase es `SUPABASE_SERVICE_ROLE_KEY`. | Unificar configuración y evitar referencias obsoletas. | `SUPABASE_SECRET_KEY`, que queda descartada/obsoleta para este proyecto. |
| 2026-09-17 | Materiales, Clientes, Proveedores y Usuarios/perfiles se mantienen bajo maestros unificados con una sola entrada de navegación. | Reducir duplicación, rutas paralelas y confusión para usuarios no técnicos. | Pestañas o accesos separados que duplican la navegación de maestros. |
| 2026-09-17 | Se integró `fix/conexion-y-limpieza-modulos` en Finanzas tras detectar divergencia de 144/6 commits sin fusión previa. | La rama de Finanzas conservaba `proxy.ts`, módulos legacy y configuración privada obsoleta; además se detectó una nueva ocurrencia del mismo problema en `src/lib/supabase/public-token-admin.ts`, creada después del fix original. | Seguir construyendo Finanzas sobre ramas hermanas divergentes o asumir que el fix ya estaba incorporado. |
| 2026-09-17 | Se confirmó que el bloqueo de campaña por instalación NO requiere concepto de ciclo: mientras exista cualquier campaña en estado `Abierta`, la instalación no puede agregarse a otra. Se corrigió `METODOLOGIA.md` para que deje de mencionar “mismo ciclo”. | El comportamiento existente del servidor y la interfaz coincide con la regla de negocio definitiva y evita duplicar abastecimiento mientras la campaña siga abierta. | Introducir una entidad/campo de ciclo o permitir campañas abiertas simultáneas por período. |
| 2026-09-17 | Se documentó `Operaciones` como séptimo rol oficial; existía en código desde antes de la divergencia de ramas y hoy no tiene usuarios asignados en Supabase. | Preservar una capacidad ya modelada y hacer explícita su existencia y alcance antes de continuar la integración. | Eliminar el rol por no tener usuarios asignados actualmente. |
| 2026-09-17 | Se conectó `InventoryModule.tsx` como pestaña independiente `Inventario` para Admin Total, Gerencia, Bodega y Operaciones. | Separar claramente Recepción de la consulta/ajuste de inventario y mantener navegación consistente con el resto del sistema. | Mantener Inventario oculto o combinado bajo la etiqueta `Recepción e inventario`. |

| 2026-09-19 | Se reafirma que NO existe concepto de ciclo para bloquear instalaciones: cualquier campaña en estado `Abierta` bloquea la instalación sin importar período. | Evitar reintroducir una regla descartada durante el rediseño de estados/navegación. | Bloqueo por “mismo ciclo” o por período. |
| 2026-09-19 | Los links de campo `/conteo/[token]` se generan siempre sobre el dominio canónico de Production `https://alemsi-materiales.vercel.app`, incluso cuando se originan desde Preview. | Un deployment Preview es efímero y no debe invalidar enlaces entregados a instalaciones. | Construir el link desde `Host`/`x-forwarded-host` del deployment que lo generó. |
| 2026-09-19 | La auditoría de permisos parte de las estructuras existentes, pero no se activa aún una matriz configurable: `user_module_permissions` y `user_scope_access` están vacías; `roleModules` sigue siendo la fuente funcional vigente hasta reconciliar servidor/RLS. | Evitar que una UI de permisos sin datos ni revalidación de servidor produzca autorizaciones inconsistentes. | Sustituir `roleModules` inmediatamente solo porque las tablas existen. |
| 2026-09-19 | PDI Angol - Cuartel 2 no presenta materiales porque su contrato PDI Angol no tiene filas en `contract_materials`, `installation_material_profiles` ni `client_materials`; no se atribuye a un bug específico del token. | El código del token consulta `contract_materials` y la fuente está vacía para ese contrato. | Modificar el lector público sin corregir primero la configuración de materiales. |
