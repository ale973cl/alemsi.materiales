"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
import {useRouter} from "next/navigation";
import {confirmRoutePrepared,startDeliveryRoute,updateDeliveryRouteOrder} from "@/app/route-actions";

export type DeliveryRoute={id:string;route_name:string;planned_date:string;status:string;preparation_assignee_id:string;delivery_assignee_id:string;created_at:string;preparation_assignee?:any;delivery_assignee?:any;delivery_route_dispatches?:any[]};
const person=(value:any)=>Array.isArray(value)?value[0]:value;
const inst=(d:any)=>Array.isArray(d?.installations)?d.installations[0]:d?.installations;
const routeLinks=(r:DeliveryRoute)=>[...(r.delivery_route_dispatches||[])].sort((a,b)=>Number(a.delivery_order)-Number(b.delivery_order));
const routeCampaign=(r:DeliveryRoute)=>{for(const x of routeLinks(r)){const o=String(x.dispatches?.observations||"");if(o.includes("CAMPAÑA:")){const parts=o.split(" · ");if(parts[1])return parts[1];}}return "Campaña no identificada"};
const clean=(value:any)=>String(value||"").trim();
const navigationAddress=(x:any)=>{const i=inst(x.dispatches)||{};const street=clean(i.address);if(!street)return "";const locality=clean(i.commune)||clean(i.city);return [street,locality,clean(i.region),"Chile"].filter(Boolean).join(", ")};
const displayAddress=(x:any)=>navigationAddress(x)||"Dirección pendiente";
const routeBatches=(r:DeliveryRoute)=>{const valid=routeLinks(r).filter(x=>Boolean(navigationAddress(x)));const batches:any[][]=[];for(let i=0;i<valid.length;i+=9)batches.push(valid.slice(i,i+9));return batches};
const mapsUrl=(batch:any[])=>{const pts=batch.map(navigationAddress).filter(Boolean);if(!pts.length)return "";const q=new URLSearchParams({api:"1",travelmode:"driving",dir_action:"navigate",destination:pts[pts.length-1]});if(pts.length>1)q.set("waypoints",pts.slice(0,-1).join("|"));return `https://www.google.com/maps/dir/?${q.toString()}`};

export default function DeliveryRoutesModule({currentUser,role,statusFilter,onRegisterDelivery}:{currentUser:{id:string;full_name?:string|null};role:string;statusFilter?:string[];onRegisterDelivery?:(dispatch:any)=>void}){
 const router=useRouter();
 const [routes,setRoutes]=useState<DeliveryRoute[]>([]),[loading,setLoading]=useState(true),[message,setMessage]=useState("");
 const [editing,setEditing]=useState<string>(""),[draft,setDraft]=useState<any[]>([]),[reason,setReason]=useState("");
 const upper=["Admin Total","Gerencia","Admin","Bodega"].includes(role);
 const loadRoutes=async()=>{setLoading(true);try{const r=await fetch("/api/delivery-routes",{cache:"no-store"});const b=await r.json();if(!r.ok)throw new Error(b.error||"No se pudieron cargar las rutas");setRoutes(b.routes||[])}catch(e:any){setMessage(e.message)}finally{setLoading(false)}};
 useEffect(()=>{void loadRoutes()},[]);
 let visible=(statusFilter?.length?routes.filter(r=>statusFilter.includes(r.status)):routes).filter(r=>r.preparation_assignee_id===currentUser.id||r.delivery_assignee_id===currentUser.id).sort((a,b)=>String(a.planned_date).localeCompare(String(b.planned_date))||String(a.created_at).localeCompare(String(b.created_at)));
 const move=(i:number,d:number)=>setDraft(old=>{const next=[...old],t=i+d;if(t<0||t>=next.length)return old;[next[i],next[t]]=[next[t],next[i]];return next});
 return <section style={{margin:"12px 0 16px",border:"1px solid #c8dce8",borderRadius:14,overflow:"hidden",background:"#fff"}}>
  <div style={{padding:"12px 14px",background:"#f5fafb",borderBottom:"1px solid #dce8ef"}}><h3 style={{margin:"0 0 4px"}}>Mis tareas de ruta</h3><small>Esta vista muestra solamente las rutas donde tu perfil está asignado para preparar o entregar. Se ordenan por fecha prevista, primero la más próxima.</small></div>
  {message&&<div className="notice" style={{margin:12}}>{message}</div>}
  <div style={{display:"grid",gap:10,padding:12}}>{loading?<div className="empty"><b>Cargando rutas…</b></div>:visible.map((r,index)=>{const links=routeLinks(r),prep=person(r.preparation_assignee),del=person(r.delivery_assignee);const canAdjust=upper||r.preparation_assignee_id===currentUser.id;const prepTask=r.preparation_assignee_id===currentUser.id;const deliveryTask=r.delivery_assignee_id===currentUser.id;const batches=routeBatches(r);const pendingAddress=links.filter(x=>!navigationAddress(x)).length;return <article key={r.id} style={{border:index===0?"2px solid #5daea2":"1px solid #c8dce8",borderRadius:12,padding:12}}><div style={{display:"flex",justifyContent:"space-between",gap:12,alignItems:"start",flexWrap:"wrap"}}><div>{index===0&&<span style={{display:"inline-block",fontSize:11,fontWeight:800,color:"#0b526f",marginBottom:4}}>PRÓXIMA RUTA</span>}<br/><b style={{fontSize:16}}>{r.route_name}</b><br/><small>{r.planned_date} · {links.length} instalaciones · {r.status}</small><br/><small>Campaña: <b>{routeCampaign(r)}</b></small><br/><small>Prepara: <b>{prep?.full_name||"—"}</b> · Entrega: <b>{del?.full_name||"—"}</b></small>{pendingAddress>0&&<><br/><small style={{fontWeight:700}}>Dirección pendiente: {pendingAddress} instalación{pendingAddress===1?"":"es"} excluida{pendingAddress===1?"":"s"} de Maps.</small></>}<div style={{marginTop:6,fontWeight:700,color:"#0b526f"}}>{prepTask&&deliveryTask?"Tu tarea: preparar y entregar":prepTask?"Tu tarea: preparar ruta":deliveryTask?"Tu tarea: entregar ruta":""}</div></div><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><Link className="btn" href={`/rutas/${r.id}/consolidado`} target="_blank">Ver ruta / consolidado</Link>{batches.map((batch,batchIndex)=>{const map=mapsUrl(batch);return map?<a key={batchIndex} className="btn" href={map} target="_blank" rel="noreferrer">{batches.length===1?"Abrir ruta en Google Maps":`Google Maps · Parte ${batchIndex+1}/${batches.length}`}</a>:null})}{canAdjust&&["Asignada","Preparada"].includes(r.status)&&<button onClick={()=>{setEditing(editing===r.id?"":r.id);setDraft(links);setReason("")}}>Adaptar orden</button>}{r.status==="Asignada"&&prepTask&&<button onClick={async()=>{try{await confirmRoutePrepared(r.id);await loadRoutes();router.refresh()}catch(e:any){setMessage(e.message)}}}>Marcar preparada</button>}{r.status==="Preparada"&&deliveryTask&&<button onClick={async()=>{try{await startDeliveryRoute(r.id);await loadRoutes();router.refresh()}catch(e:any){setMessage(e.message)}}}>Iniciar ruta</button>}</div></div>{editing===r.id&&<div style={{marginTop:12,padding:12,background:"#f8fbfc",borderRadius:10,border:"1px solid #dce8ef"}}><b>Adaptación de orden</b><p style={{margin:"4px 0 10px",fontSize:12}}>Si quien prepara detecta que el recorrido debe cambiar, puede reordenarlo. La justificación queda en auditoría.</p>{draft.map((x:any,i)=><div key={x.id} style={{display:"grid",gridTemplateColumns:"30px 1fr auto",gap:8,alignItems:"center",padding:"7px 0",borderBottom:"1px solid #e5eef2"}}><b>{i+1}</b><span>{inst(x.dispatches)?.name||"Instalación"}</span><span style={{display:"flex",gap:4}}><button disabled={i===0} onClick={()=>move(i,-1)}>↑</button><button disabled={i===draft.length-1} onClick={()=>move(i,1)}>↓</button></span></div>)}<textarea value={reason} onChange={e=>setReason(e.target.value)} placeholder="Justificación obligatoria de la adaptación" style={{width:"100%",marginTop:10,minHeight:70}}/><div style={{display:"flex",justifyContent:"flex-end",gap:8,marginTop:8}}><button onClick={()=>setEditing("")}>Cancelar</button><button onClick={async()=>{try{await updateDeliveryRouteOrder(r.id,draft.map((x:any)=>String(x.dispatch_id)),reason);setEditing("");setMessage("Orden de ruta actualizado y justificado.");await loadRoutes();router.refresh()}catch(e:any){setMessage(e.message)}}}>Guardar adaptación</button></div></div>}<details style={{marginTop:10}} open={r.status==="En tránsito"}><summary>Instalaciones de la ruta</summary><div style={{display:"grid",gap:6,marginTop:8}}>{links.map((x:any,i)=>{const d=x.dispatches,irow=inst(d)||{};return <div key={x.id} style={{display:"grid",gridTemplateColumns:"30px minmax(180px,1fr) auto",gap:8,alignItems:"center",padding:"8px 0",borderBottom:"1px solid #e7eff3"}}><b>{i+1}</b><span><b>{irow.name||"Instalación"}</b><br/><small>{displayAddress(x)} · {d?.status||""}</small></span><span style={{display:"flex",gap:6,flexWrap:"wrap"}}><Link className="btn" href={`/despachos/${d?.id}/guia`} target="_blank">Ver guía</Link>{onRegisterDelivery&&deliveryTask&&["En tránsito","Entrega parcial"].includes(String(d?.status||""))&&<button onClick={()=>onRegisterDelivery(d)}>{d?.status==="Entrega parcial"?"Completar entrega":"Registrar entrega"}</button>}</span></div>})}</div></details></article>})}{!loading&&!visible.length&&<div className="empty"><b>Sin tareas de ruta asignadas</b><span>Cuando te designen una ruta para preparar o entregar, aparecerá aquí.</span></div>}</div>
 </section>;
}
