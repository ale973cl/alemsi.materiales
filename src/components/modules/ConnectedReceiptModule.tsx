"use client";
import {useEffect,useMemo,useState} from "react";
import {registerPurchaseOrderReceipt} from "@/app/receipt-actions";
type Line={id:string;material_id:string|null;description?:string|null;unit?:string|null;supplier_code?:string|null;ordered_qty:number;unit_net_price:number;already_received:number;pending_qty:number;materials?:{name?:string|null;presentation?:string|null;unit?:string|null;supplier_code?:string|null}|null};
type Order={id:string;order_number?:string|null;status:string;total_net:number;order_type?:string|null;suppliers?:{legal_name?:string|null}|null;purchase_order_lines:Line[]};
type Receipt={id:string;purchase_order_id?:string|null;status:string;received_at?:string|null;shipping_condition?:string|null;freight_net?:number|null;document_type?:string|null;document_folio?:string|null;document_date?:string|null;reconciliation_status?:string|null};
const money=(n:number)=>new Intl.NumberFormat("es-CL",{style:"currency",currency:"CLP",maximumFractionDigits:0}).format(n||0);
export default function ConnectedReceiptModule({role}:{role:string}){
 const [orders,setOrders]=useState<Order[]>([]),[receipts,setReceipts]=useState<Receipt[]>([]),[selectedId,setSelectedId]=useState("");
 const [qty,setQty]=useState<Record<string,number>>({}),[prices,setPrices]=useState<Record<string,number>>({});
 const [documentType,setDocumentType]=useState("Factura"),[documentFolio,setDocumentFolio]=useState(""),[documentDate,setDocumentDate]=useState(""),[documentNet,setDocumentNet]=useState(0),[documentVat,setDocumentVat]=useState(0),[documentTotal,setDocumentTotal]=useState(0);
 const [shipping,setShipping]=useState("Incluido"),[freight,setFreight]=useState(0),[observation,setObservation]=useState(""),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[message,setMessage]=useState("");
 const canReceive=["Admin Total","Admin","Bodega"].includes(role);
 const load=async()=>{setLoading(true);try{const response=await fetch("/api/receipts/context",{cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body.error||"No se pudo cargar Recepción");setOrders(body.orders||[]);setReceipts(body.receipts||[])}catch(error:any){setMessage(error?.message||"No se pudo cargar Recepción")}finally{setLoading(false)}};
 useEffect(()=>{void load()},[]);
 const receivable=useMemo(()=>orders.filter(order=>(order.purchase_order_lines||[]).some(line=>line.material_id&&Number(line.pending_qty)>0)),[orders]);
 const selected=receivable.find(order=>order.id===selectedId)||null;const materialLines=(selected?.purchase_order_lines||[]).filter(line=>line.material_id&&Number(line.pending_qty)>0);
 useEffect(()=>{if(!selected){setQty({});setPrices({});return}const q:Record<string,number>={},p:Record<string,number>={};for(const line of materialLines){q[line.id]=Number(line.pending_qty||0);p[line.id]=Number(line.unit_net_price||0)}setQty(q);setPrices(p);setDocumentType("Factura");setDocumentFolio("");setDocumentDate(new Date().toISOString().slice(0,10));setDocumentNet(Number(selected.total_net||0));setDocumentVat(Math.round(Number(selected.total_net||0)*.19));setDocumentTotal(Math.round(Number(selected.total_net||0)*1.19));setShipping("Incluido");setFreight(0);setObservation("");setMessage("")},[selectedId]);
 const submit=async()=>{if(!selected)return;const lines=materialLines.map(line=>({purchase_order_line_id:line.id,material_id:line.material_id,received_qty:Number(qty[line.id]||0),actual_unit_net:Number(prices[line.id]||0)})).filter(line=>line.received_qty>0);if(!documentFolio.trim()){setMessage("Ingresa el folio del documento");return}if(!documentDate){setMessage("Ingresa la fecha del documento");return}if(!lines.length){setMessage("Ingresa al menos una cantidad recibida");return}if(shipping==="Por pagar"&&freight<=0){setMessage("Ingresa el valor neto del flete por pagar");return}const formData=new FormData();formData.set("purchase_order_id",selected.id);formData.set("lines",JSON.stringify(lines));formData.set("document_type",documentType);formData.set("document_folio",documentFolio);formData.set("document_date",documentDate);formData.set("document_net",String(documentNet));formData.set("document_vat",String(documentVat));formData.set("document_total",String(documentTotal));formData.set("shipping_condition",shipping);formData.set("freight_net",String(shipping==="Por pagar"?freight:0));formData.set("observation",observation);setSaving(true);setMessage("");try{await registerPurchaseOrderReceipt(formData);setMessage("Documento y recepción registrados. Quedan pendientes de cotejo en Finanzas; el inventario aún no se actualiza.");setSelectedId("");await load()}catch(error:any){setMessage(error?.message||"No se pudo registrar la recepción")}finally{setSaving(false)}};
 return <section className="panel receiptModule"><div className="receiptHead"><div><h2>Recepción de materiales</h2><p>Registra la recepción física junto al documento que la respalda. Finanzas coteja documento vs OC antes de cargar inventario.</p></div><span className="catalogCount">{receivable.length} OC con saldo</span></div>{!canReceive&&<p className="note"><b>Modo consulta.</b> Tu perfil puede revisar recepciones, pero no registrar ingreso físico.</p>}{loading?<div className="empty"><b>Cargando recepción…</b></div>:<><div className="receiptSelector"><label>Orden de compra<select value={selectedId} onChange={e=>setSelectedId(e.target.value)} disabled={!canReceive}><option value="">Seleccionar OC con materiales pendientes</option>{receivable.map(order=><option key={order.id} value={order.id}>{order.order_number||"OC sin folio"} · {order.suppliers?.legal_name||"Proveedor"} · {order.status}</option>)}</select></label></div>{selected&&<div className="receiptEditor"><div className="receiptSummary"><span><small>OC</small><b>{selected.order_number||"Sin folio"}</b></span><span><small>Proveedor</small><b>{selected.suppliers?.legal_name||"Proveedor"}</b></span><span><small>Estado</small><b>{selected.status}</b></span><span><small>Neto OC</small><b>{money(Number(selected.total_net||0))}</b></span></div><div className="receiptDocument"><label>Tipo de documento<select value={documentType} onChange={e=>setDocumentType(e.target.value)}><option>Factura</option><option>Boleta</option><option>Guía de despacho</option><option>Otro</option></select></label><label>Folio documento<input value={documentFolio} onChange={e=>setDocumentFolio(e.target.value)} placeholder="Folio obligatorio"/></label><label>Fecha documento<input type="date" value={documentDate} onChange={e=>setDocumentDate(e.target.value)}/></label><label>Neto documento<input type="number" min="0" value={documentNet} onChange={e=>setDocumentNet(Number(e.target.value))}/></label><label>IVA<input type="number" min="0" value={documentVat} onChange={e=>setDocumentVat(Number(e.target.value))}/></label><label>Total documento<input type="number" min="0" value={documentTotal} onChange={e=>setDocumentTotal(Number(e.target.value))}/></label></div><div className="receiptTable"><div className="receiptRow receiptHeader"><span>Material</span><span>Pedido</span><span>Recibido</span><span>Pendiente</span><span>Ingreso ahora</span><span>Valor neto unit.</span></div>{materialLines.map(line=>{const name=line.materials?.name||line.description||"Material";return <div className="receiptRow" key={line.id}><span><b>{name}</b><small>{line.materials?.presentation||line.materials?.unit||line.unit||""}</small></span><span>{Number(line.ordered_qty||0)}</span><span>{Number(line.already_received||0)}</span><span><b>{Number(line.pending_qty||0)}</b></span><span><input type="number" min="0" max={Number(line.pending_qty||0)} step="any" value={qty[line.id]??0} onChange={e=>setQty(current=>({...current,[line.id]:Number(e.target.value)}))}/></span><span><input type="number" min="0" step="any" value={prices[line.id]??0} onChange={e=>setPrices(current=>({...current,[line.id]:Number(e.target.value)}))}/></span></div>})}</div><div className="receiptShipping"><label>Condición de envío<select value={shipping} onChange={e=>{setShipping(e.target.value);if(e.target.value!=="Por pagar")setFreight(0)}}><option>Incluido</option><option>Por pagar</option><option>Retiro</option></select></label><label>Flete neto<input type="number" min="0" step="1" value={freight} disabled={shipping!=="Por pagar"} onChange={e=>setFreight(Number(e.target.value))}/></label><label className="receiptObservation">Observación<textarea rows={3} value={observation} onChange={e=>setObservation(e.target.value)} placeholder="Diferencias, estado de bultos, referencia de transporte, etc."/></label></div>{message&&<p className="note"><b>{message}</b></p>}<button type="button" onClick={submit} disabled={saving}>{saving?"Registrando…":"Registrar para cotejo"}</button></div>}<div className="receiptHistory"><h3>Últimas recepciones/documentos</h3>{receipts.length?receipts.slice(0,12).map(receipt=>{const order=orders.find(o=>o.id===receipt.purchase_order_id);return <div className="receiptHistoryRow" key={receipt.id}><span><b>{receipt.document_type||"Documento"} {receipt.document_folio||"sin folio"}</b><small>{order?.order_number||"Sin OC"} · {order?.suppliers?.legal_name||"Proveedor"}</small></span><span>{receipt.reconciliation_status||receipt.status}</span><span>{receipt.shipping_condition||"Incluido"}</span><strong>{Number(receipt.freight_net||0)>0?`${money(Number(receipt.freight_net))} flete neto`:"Sin flete imputado"}</strong></div>}):<div className="empty"><b>Aún no hay recepciones registradas</b></div>}</div></>}
 <style jsx>{`.receiptHead{display:flex;justify-content:space-between;gap:20px;align-items:flex-start}.receiptHead h2{margin:0 0 6px}.receiptHead p{margin:0}.receiptSelector{margin:18px 0}.receiptSelector label,.receiptShipping label,.receiptDocument label{display:flex;flex-direction:column;gap:6px;font-weight:700}.receiptSelector select,.receiptShipping select,.receiptShipping input,.receiptShipping textarea,.receiptDocument select,.receiptDocument input,.receiptRow input{width:100%;box-sizing:border-box}.receiptSummary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-bottom:16px}.receiptSummary span{border:1px solid #dfe6ea;border-radius:10px;padding:10px;display:flex;flex-direction:column}.receiptDocument{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;margin-bottom:16px}.receiptTable{overflow:auto;border:1px solid #dfe6ea;border-radius:12px}.receiptRow{display:grid;grid-template-columns:minmax(240px,2fr) repeat(5,minmax(95px,1fr));gap:10px;align-items:center;padding:10px 12px;border-bottom:1px solid #e7ecef;min-width:900px}.receiptRow>span:first-child{display:flex;flex-direction:column}.receiptHeader{font-weight:800;background:#f4f7f8}.receiptShipping{display:grid;grid-template-columns:1fr 1fr 2fr;gap:12px;margin:18px 0}.receiptHistory{margin-top:26px}.receiptHistoryRow{display:grid;grid-template-columns:2fr 1fr 1fr 1.4fr;gap:12px;align-items:center;padding:10px 0;border-bottom:1px solid #e7ecef}.receiptHistoryRow span:first-child{display:flex;flex-direction:column}.receiptHistoryRow strong{text-align:right}@media(max-width:900px){.receiptSummary,.receiptDocument{grid-template-columns:1fr 1fr}.receiptShipping{grid-template-columns:1fr}.receiptHistoryRow{grid-template-columns:1fr 1fr}.receiptHistoryRow strong{text-align:left}}@media(max-width:600px){.receiptHead{display:block}.receiptSummary,.receiptDocument{grid-template-columns:1fr}}`}</style></section>;
}
