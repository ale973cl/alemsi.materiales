"use client";
import type {ClientProfile,Dispatch,MaterialState} from "@/lib/materiales-domain";
export default function DispatchModule({state,setState,profile}:{state:MaterialState;setState:(x:MaterialState)=>void;profile:ClientProfile}){const items=state.dispatches.filter(d=>d.profileId===profile.id);const reg=(d:Dispatch)=>{const req=d.lines.reduce((s,l)=>s+l.required,0),cur=d.lines.reduce((s,l)=>s+l.delivered,0),q=Number(prompt("Cantidad total entregada:",String(cur))||0);let rem=q;const lines=d.lines.map(l=>{const delivered=Math.min(l.required,Math.max(0,rem));rem-=delivered;return {...l,delivered}}),del=lines.reduce((s,l)=>s+l.delivered,0),status:Dispatch["status"]=del>=req?"Cerrada":del>0?"Parcial":"Pendiente";setState({...state,dispatches:state.dispatches.map(x=>x.id===d.id?{...x,lines,status}:x)})};return <><h1>Guías / Despachos</h1><div className="mobileCards">{items.length?items.map(d=>{const req=d.lines.reduce((s,l)=>s+l.required,0),del=d.lines.reduce((s,l)=>s+l.delivered,0);return <div className="installCard" key={d.id}><div><b>{d.installationName}</b><span>Requerido {req} · Entregado {del}</span><small>Pendiente {req-del}</small></div><span className={`pill ${d.status==="Cerrada"?"ok":"warn"}`}>{d.status}</span><button className="btn primary large" onClick={()=>reg(d)}>Registrar guía</button></div>}):<div className="empty">Sin despachos.</div>}</div></>}
