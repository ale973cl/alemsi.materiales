import {NextResponse} from "next/server";
import {createClient} from "@/lib/supabase/server";
export async function GET(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"No autorizado"},{status:401});const {data:profile}=await supabase.from("user_profiles").select("role,active").eq("id",user.id).single();if(!profile?.active)return NextResponse.json({error:"Usuario inactivo"},{status:403});const [{data:receipts,error:rError},{data:suppliers,error:sError},{data:materials,error:mError}]=await Promise.all([supabase.from("receipts").select("id,purchase_order_id,supplier_id,document_type,document_folio,document_date,document_net,vat_amount,document_total,no_oc_reason,reconciliation_status,inventory_posted,received_at,reconciled_at,suppliers(legal_name,rut),purchase_orders(order_number,total_net,status),receipt_lines(id,purchase_order_line_id,material_id,received_qty,actual_unit_net,line_net,materials(name,family,presentation,unit,supplier_code))").order("received_at",{ascending:false}),supabase.from("suppliers").select("id,legal_name,rut").eq("active",true).order("legal_name"),supabase.from("materials").select("id,name,family,presentation,unit,supplier_code,current_net_price").eq("active",true).order("name")]);if(rError)return NextResponse.json({error:rError.message},{status:500});if(sError)return NextResponse.json({error:sError.message},{status:500});if(mError)return NextResponse.json({error:mError.message},{status:500});return NextResponse.json({receipts:receipts||[],suppliers:suppliers||[],materials:materials||[],role:profile.role});}
