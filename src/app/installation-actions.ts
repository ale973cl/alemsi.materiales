"use server";
import {revalidatePath} from "next/cache";
import {createClient} from "@/lib/supabase/server";
async function ctx(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Sesión no válida");const {data:profile}=await supabase.from("user_profiles").select("role,active,full_name,email").eq("id",user.id).single();if(!profile?.active||!["Admin Total","Gerencia","Admin"].includes(profile.role))throw new Error("No autorizado");return{supabase,user,profile}}
export async function saveInstallationDetails(formData:FormData){const {supabase,user,profile}=await ctx();const id=String(formData.get("installation_id")||"");const name=String(formData.get("name")||"").trim();const region=String(formData.get("region")||"").trim()||null;const city=String(formData.get("city")||"").trim()||null;const commune=String(formData.get("commune")||"").trim()||null;const raw=String(formData.get("collaborator_count")||"").trim();const collaborator_count=raw===""?null:Number(raw);if(!id||!name)throw new Error("Instalación y nombre son obligatorios");if(collaborator_count!==null&&(!Number.isInteger(collaborator_count)||collaborator_count<0))throw new Error("La cantidad de colaboradoras debe ser un entero igual o mayor a 0");const {data:old}=await supabase.from("installations").select("id,name,region,city,commune,collaborator_count").eq("id",id).single();const {data,error}=await supabase.from("installations").update({name,region,city,commune,collaborator_count,updated_at:new Date().toISOString()}).eq("id",id).select("id,name,region,city,commune,collaborator_count").single();if(error)throw error;await supabase.from("activity_log").insert({actor_id:user.id,actor_name:profile.full_name||profile.email,module:"Clientes e instalaciones",action:"Editar instalación y dotación",entity_table:"installations",entity_id:id,old_data:old,new_data:data});revalidatePath("/");}
