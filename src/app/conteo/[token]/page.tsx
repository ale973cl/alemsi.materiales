import {createHash} from "crypto";
import {createClient} from "@supabase/supabase-js";
import TokenSurveyForm from "./TokenSurveyForm";

function hash(token:string){return createHash("sha256").update(token).digest("hex")}
function db(){if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.SUPABASE_SECRET_KEY)return null;return createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SECRET_KEY,{auth:{persistSession:false}})}
export default async function Page({params}:{params:Promise<{token:string}>}){const {token}=await params;const supabase=db();if(!supabase)return <main style={{maxWidth:760,margin:"40px auto",padding:20}}><h1>Conteo no disponible</h1></main>;const {data:m}=await supabase.from("campaign_installations").select("campaign_id,installation_id,status,survey_token_expires_at,survey_token_used_at,installations(name,region,contract_id,contracts(name,clients(legal_name)))").eq("survey_token_hash",hash(token)).maybeSingle();if(!m||m.survey_token_used_at||m.status==="Completada"||!m.survey_token_expires_at||new Date(m.survey_token_expires_at).getTime()<Date.now())return <main style={{maxWidth:760,margin:"40px auto",padding:20,fontFamily:"Arial,sans-serif"}}><h1>Link no disponible</h1><p>El enlace expiró, ya fue utilizado o fue reemplazado. Solicita uno nuevo a ALEMSI.</p></main>;const inst:any=Array.isArray((m as any).installations)?(m as any).installations[0]:(m as any).installations;const {data:campaign}=await supabase.from("campaigns").select("status,label").eq("id",m.campaign_id).single();if(!campaign||campaign.status!=="Abierta")return <main style={{maxWidth:760,margin:"40px auto",padding:20}}><h1>Campaña cerrada</h1></main>;const {data:raw}=await supabase.from("contract_materials").select("material_id,installation_id,authorized_qty,materials(name,presentation,unit)").eq("contract_id",inst?.contract_id).eq("authorized",true).or(`installation_id.is.null,installation_id.eq.${m.installation_id}`);const by=new Map<string,any>();for(const item of raw||[]){if(!by.has(item.material_id)||item.installation_id===m.installation_id)by.set(item.material_id,item)}const materials=[...by.values()].map((x:any)=>{const mat=Array.isArray(x.materials)?x.materials[0]:x.materials;return{material_id:x.material_id,name:mat?.name||"Material",presentation:mat?.presentation||null,unit:mat?.unit||null,authorized_qty:Number(x.authorized_qty||0)}});const contract:any=Array.isArray(inst?.contracts)?inst.contracts[0]:inst?.contracts;const client:any=Array.isArray(contract?.clients)?contract.clients[0]:contract?.clients;return <main style={{maxWidth:820,margin:"0 auto",padding:24,fontFamily:"Arial,sans-serif",color:"#12314a"}}><header style={{marginBottom:22,borderBottom:"2px solid #0b6f69",paddingBottom:14}}><p style={{margin:0,color:"#0b6f69",fontWeight:700}}>ALEMSI MATERIALES</p><h1 style={{margin:"6px 0"}}>Conteo de materiales</h1><p style={{margin:0}}><b>{client?.legal_name||"Cliente"}</b> · {contract?.name||"Contrato"} · {inst?.name||"Instalación"}</p></header>{materials.length?<TokenSurveyForm token={token} materials={materials} installation={inst?.name||"Instalación"}/>:<p>No hay materiales autorizados para esta instalación.</p>}</main>}
